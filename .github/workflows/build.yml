on:
  push:
    branches:
    - main
    - master

jobs:
  build-job:
    name: build the jar
    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v1
      name: checkout repo
      
    - name: Build with maven
      uses: mvn clean install
      
